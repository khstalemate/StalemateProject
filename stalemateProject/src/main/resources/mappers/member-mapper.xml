<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.stale.mate.member.model.mapper.MemberMapper">

	<select id="login">
		SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME,
		MEMBER_PW, AUTHORITY, PROFILE_IMG,
		ENROLL_DATE, MEMBER_PHONE
		FROM "MEMBER"
		WHERE MEMBER_ID = #{memberId}
		AND MEMBER_DEL_FL = 'N' 
	</select>
	
	<select id="checkId" resultType="int">
		SELECT COUNT(*)
		FROM "MEMBER"
		WHERE MEMBER_ID = #{memberId}
		AND MEMBER_DEL_FL = 'N'
	</select>
	
	<select id="checkName" resultType="int">
		SELECT COUNT(*)
		FROM "MEMBER"
		WHERE MEMBER_NAME = #{memberName}
		AND MEMBER_DEL_FL = 'N'
	</select>
	
	<insert id="signup">
		INSERT INTO "MEMBER"
		VALUES(SEQ_MEMBER.NEXTVAL,
			   #{memberId},
			   #{memberName},
			   #{memberPw},
			   #{memberPhone},
			   DEFAULT,
			   DEFAULT,
			   NULL,
			   DEFAULT,
			   NULL)
	</insert>
	
	<select id="selectMemberByNo">
   	 	SELECT MEMBER_NO, MEMBER_ID, MEMBER_NAME,
        MEMBER_PW, AUTHORITY, PROFILE_IMG
   		FROM "MEMBER"
   		WHERE MEMBER_NO = #{memberNo}
     	AND MEMBER_DEL_FL = 'N'	
	</select>
</mapper>